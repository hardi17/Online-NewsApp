<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hardi.newsapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.hardi.NewsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".ui.searchactivity.SearchActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.languageactivity.LanguagesActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.countryactivity.CountriesActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.newslist.NewsListActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.newssources.NewsSourcesActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.topheadline.TopHeadlineActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.mainactivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>